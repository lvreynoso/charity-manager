<!-- transactions-new.handlebars -->

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <form action="/accounts/{{account.slug}}/transactions" method="post">
                <input type="hidden" name="account" value={{account._id}}>
                <legend>New Transaction for {{account.name.full}}</legend>
                {{> transaction-form charity}}
            </form>
        </div>
    </div>
</div>
